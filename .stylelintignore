###############################################################################
# MCP.JMRP.IO — STYLELINT IGNORE
###############################################################################
# Build output and vendored/generated trees. `builds/` is where the blue/green
# colours live, so it holds two full copies of the site.

dist/
builds/
node_modules/
.astro/
public/
test-results/
playwright-report/
ops/
plan/
